# Example environment variables for the GRC Assistant (Vite)
# Copy this file to `.env.local` and fill in your real secrets. Never commit `.env.local`.

# API host for the authentication and chat endpoints (no trailing slash)

# OAuth / Auth host (Cognito domain) used to request tokens. Example:
# https://cybersec-rag-domain.auth.eu-west-1.amazoncognito.com
VITE_AUTH_HOST=https://cybersec-rag-domain.auth.eu-west-1.amazoncognito.com

# Chat API host (API Gateway) used for chat requests. Example:
# https://cfpu8q1wol.execute-api.eu-west-1.amazonaws.com
VITE_API_HOST=https://cfpu8q1wol.execute-api.eu-west-1.amazonaws.com

# OAuth2 client credentials used to obtain a Bearer token
VITE_CLIENT_ID=your_client_id_here
VITE_CLIENT_SECRET=your_client_secret_here

# API key for the chat endpoint (x-api-key header)
VITE_API_KEY=your_api_key_here

# Optional: change the session prefix or other runtime flags
# VITE_APP_NAME=GRC Assistant

# Notes:
# - Vite requires env vars exposed to client-side code to be prefixed with VITE_.
# - Put real secrets in `.env.local` (or in your CI environment) and do not commit them.


# --- Backend Environment Variables ---

# Server Configuration
PORT=3001
FRONTEND_ORIGIN=http://localhost:8080

# Shared Credentials (backend versions)
AUTH_HOST=https://cybersec-rag-domain.auth.eu-west-1.amazoncognito.com
CLIENT_ID=your_client_id_here
CLIENT_SECRET=your_client_secret_here
API_HOST=https://cfpu8q1wol.execute-api.eu-west-1.amazonaws.com
API_KEY=your_api_key_here

# Microsoft Azure AD OAuth Credentials
MICROSOFT_CLIENT_ID="your_microsoft_client_id"
MICROSOFT_CLIENT_SECRET="your_microsoft_client_secret"
MICROSOFT_TENANT_ID="your_microsoft_tenant_id" # or 'common'
# This should match the backend callback URL, e.g., http://localhost:4000/api/auth/microsoft/callback
MICROSOFT_REDIRECT_URI="http://localhost:4000/api/auth/microsoft/callback"

# Secrets for session and token management
SESSION_SECRET="a_very_secret_and_long_string_for_sessions"
JWT_SECRET="a_very_secret_and_long_string_for_jwt"

# --- Database Environment Variables ---
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=grc_user
POSTGRES_PASSWORD=grc_password
POSTGRES_DB=grc_db

# --- MinIO S3 Storage Environment Variables ---
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=grc-documents
MINIO_USE_SSL=false